{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "_note": "Non-secret service topology defaults used by the health checks. The Postgres connection string carries credentials and is NOT stored here — it is supplied via env/Vault (ConnectionStrings__Postgres).",
  "Redis": {
    "Configuration": "redis:6379"
  },
  "Minio": {
    "Endpoint": "minio:9000"
  },
  "Vault": {
    "Address": "http://vault:8200"
  },
  "Embeddings": {
    "BaseUrl": "http://embeddings:11434"
  }
}
