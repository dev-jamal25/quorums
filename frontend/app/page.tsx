// Placeholder app shell. Routes (onboarding, analytics, approvals, run/trace
// viewer) and their data come from the API; no business logic lives here.
export default function HomePage() {
  return (
    <main>
      <h1>Quorums</h1>
      <p>Dashboard shell — scaffold only.</p>
    </main>
  );
}
